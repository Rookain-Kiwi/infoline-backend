apiVersion: v1
kind: Service
metadata:
  name: infoline-backend-service
  namespace: infoline-backend
  labels:
    app: infoline-backend
spec:
  type: ClusterIP
  selector:
    app: infoline-backend
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
